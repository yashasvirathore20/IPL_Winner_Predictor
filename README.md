# IPL_Winner_Predictor
A machine learning–powered web application that predicts IPL match outcomes and simulates complete tournament standings. The project includes end-to-end data preprocessing, model training using Logistic Regression, and custom match simulation logic. 
